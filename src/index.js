import './styles/index.scss';

const rangeSliderData = {
  size: 'big'
};

const rangeSlider = {
  title: 'range-slider',
  ...rangeSliderData
};

$('.block').html('jQuery is working');
console.log(rangeSlider);